# 零调整处理链修复

对应排查报告 `zero-adjustment-pipeline-review-2026-09-07.md`。

- 比较图及其直方图独立请求中性参数，不再保存首次增强帧；记住调整、LUT 和 AI 模式均不进入基准。
- CPU/WebGL SDR 预览直接显示原生 SDR 底图，取消二次高光压缩和 HDR 模拟；真实 WebGPU HDR 显示仍使用 HDR 数据。零增益白色保持 255。
- RAW 自动测光采用固定 2.5 档捕获预算，独立于 HDR 范围和强度。SDR 输出保留同一底图曲线，不再切换肩部参数。
- 面板隐藏的对比度/自然饱和度改为 1/0；pop=0 不再增加局部清晰度或高光趋白。RAW 仍需要基础自动曝光、白平衡、去马赛克及明暗显示映射，亮度 0 表示对此基础显影不加曝光补偿。
- 无 LUT 的共同 RGB 增益保留原始浮点网格，编码时直接量化；实际 LUT 调色改变关系后仍由最终图像推导网格，零 LUT 强度保留直通路径。
- 重置全部恢复中性手动调整并清除 LUT、关闭 AI 效果；输出格式保留。新照片初始 +0.6 EV / HDR 强度 0.4 的增强预设与重置语义分开。

验证：Release 构建 HyperDR；六项原生检查通过（rendition、preview、color_lut、display_referred、look_pipeline、render_regression）；37 项 Python 面板/配置/输出检查通过；JS SDR 预览及中性设置保存恢复检查通过。

新增原生回归覆盖报告中的 256×128 明暗边界：HDR 范围 2.5→0 时 RAW 曝光与底图严格相同；SDR/HDR 输出底图一致。RAW 和 SDR 渐变在强度 0、0.4、1 下，与保留的旧网格入口比较，底图严格相同，HDR 每通道误差小于 1e-5。

未进行物理 HDR 屏幕视觉验收。首次载入增加一次独立基准预览请求，后续调整复用比较图。
